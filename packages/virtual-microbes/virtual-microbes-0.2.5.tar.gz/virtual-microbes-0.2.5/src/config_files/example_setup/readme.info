Given in this folder are an example environment, and 2 example cells that regulate their genes depending on the concentration of molecule B. Also working config files are given.

To run, setup a path to the example folder and use the following startup:
> path=/home/user/virtualmicrobes/virtualmicrobes/src/config_files/example_setup
> virtualmicrobes.py --env-from-file ${path}/environment.env @${path}/general_options.cfg evo @${path}/evo_options.cfg --cells-from-files ${path}/cell_1.cell ${path}/cell_WGD.cell --name Virtual_Microbes_Example
