from setuptools import setup

setup(name='pipelining',
      version='0.10.1',
      python_requires='>=3',
      description='Declarative pipelines on top of python multiprocessing artifacts',
      packages=['pipelining'],
      install_requires=[],
      zip_safe=False,
    )