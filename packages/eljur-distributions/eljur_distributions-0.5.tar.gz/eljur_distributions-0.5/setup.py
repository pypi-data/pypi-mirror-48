from setuptools import setup

setup(name='eljur_distributions',
      version='0.5',
      description='Gaussian and Binomial distributions',
      packages=['eljur_distributions'],
      zip_safe=False)
