from setuptools import setup

setup(name='distributions_lly',
      version='0.1',
      description='Gaussian and Binomial distributions',
      packages=['distributions_lly'],
      zip_safe=False)
