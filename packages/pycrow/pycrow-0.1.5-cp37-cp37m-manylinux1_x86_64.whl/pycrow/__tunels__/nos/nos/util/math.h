#ifndef GXX_UTIL_MATH_H
#define GXX_UTIL_MATH_H

#include <assert.h>
#include <stdint.h>
#include <sys/cdefs.h>
#include <stdbool.h>

#define __MAX__(a,b) ((a) > (b) ? (a) : (b))
#define __MIN__(a,b) ((a) < (b) ? (a) : (b))

#endif