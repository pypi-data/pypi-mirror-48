#!/bin/bash

docker build -t ${DOCKER_REPO}:${TRAVIS_TAG} -t ${DOCKER_REPO}:latest .
docker run -t ${DOCKER_REPO}:latest thelper --version  # just to run import checks
if [ "$TRAVIS_PULL_REQUEST" == "false" ]; then docker push ${DOCKER_REPO}; fi
